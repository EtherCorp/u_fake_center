# Medical Center Faker

# Dependencies
To run this script you need to install faker gem

- `gem install faker`

# How to use it

The requests should be configured in a file named `config.yml` and placed in the root folder. 
We provide a default configuration in `config.yml.default`. Keep it.

Then just run main.rb 

- `ruby main.rb`
